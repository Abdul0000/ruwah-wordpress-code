(()=>{'use strict';document.documentElement.classList.add('rwb-js');const b=document.body,q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];let last=null;const open=e=>{if(!e)return;last=document.activeElement;e.hidden=false;requestAnimationFrame(()=>e.classList.add('is-open'));b.classList.add('rwb-lock');setTimeout(()=>q('button,input,a[href]',e)?.focus(),30)},close=e=>{if(!e||e.hidden)return;e.classList.remove('is-open');setTimeout(()=>e.hidden=true,220);b.classList.remove('rwb-lock');last?.focus?.()};const menu=q('[data-menu]'),search=q('[data-search]'),cart=q('[data-cart]'),mega=q('[data-shop-menu]');q('[data-menu-open]')?.addEventListener('click',()=>open(menu));q('[data-menu-close]')?.addEventListener('click',()=>close(menu));qa('[data-search-open]').forEach(x=>x.addEventListener('click',()=>open(search)));qa('[data-search-close]').forEach(x=>x.addEventListener('click',()=>close(search)));qa('[data-cart-open]').forEach(x=>x.addEventListener('click',()=>open(cart)));qa('[data-cart-close]').forEach(x=>x.addEventListener('click',()=>close(cart)));q('[data-shop-toggle]')?.addEventListener('click',()=>{mega.hidden=!mega.hidden});document.addEventListener('keydown',e=>{if(e.key==='Escape'){close(menu);close(search);close(cart);if(mega)mega.hidden=true}});q('[data-header]')&&addEventListener('scroll',()=>q('[data-header]').classList.toggle('compact',scrollY>20),{passive:true});if(window.jQuery)window.jQuery(document.body).on('added_to_cart',()=>open(cart));const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,els=qa('[data-reveal]');if(!reduced&&'IntersectionObserver'in window){const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');o.unobserve(e.target)}}),{threshold:.1});els.forEach(e=>o.observe(e))}else els.forEach(e=>e.classList.add('visible'))})();