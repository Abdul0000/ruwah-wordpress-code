(()=>{const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)],body=document.body,header=q('[data-site-header]'),overlay=q('[data-overlay]'),drawer=q('[data-cart-drawer]'),search=q('[data-search]'),menu=q('[data-menu]'),menuButton=q('[data-menu-toggle]');let lastTrigger=null;const searchOpen=()=>Boolean(search&&search.getAttribute('aria-hidden')==='false'),cartOpen=()=>Boolean(drawer&&drawer.classList.contains('is-open')),sync=()=>{const open=cartOpen()||searchOpen();if(overlay)overlay.hidden=!open;body.classList.toggle('rb-modal-open',open)},closeMenu=()=>{if(!menu)return;menu.classList.remove('is-open');menuButton?.setAttribute('aria-expanded','false')},closeSearch=(restore=true)=>{if(!search)return;search.hidden=true;search.setAttribute('aria-hidden','true');sync();if(restore&&lastTrigger instanceof HTMLElement)lastTrigger.focus()},closeCart=(restore=true)=>{if(!drawer)return;drawer.classList.remove('is-open');drawer.setAttribute('aria-hidden','true');sync();if(restore&&lastTrigger instanceof HTMLElement)lastTrigger.focus()},openSearch=trigger=>{if(!search)return;lastTrigger=trigger||document.activeElement;closeCart(false);closeMenu();search.hidden=false;search.setAttribute('aria-hidden','false');sync();setTimeout(()=>q('input[type="search"]',search)?.focus(),30)},openCart=trigger=>{if(!drawer)return;lastTrigger=trigger||document.activeElement;closeSearch(false);closeMenu();drawer.classList.add('is-open');drawer.setAttribute('aria-hidden','false');sync();setTimeout(()=>q('[data-cart-close]',drawer)?.focus(),30)};qa('[data-cart-open]').forEach(b=>b.addEventListener('click',()=>openCart(b)));q('[data-cart-close]')?.addEventListener('click',()=>closeCart());qa('[data-search-open]').forEach(b=>b.addEventListener('click',()=>openSearch(b)));q('[data-search-close]')?.addEventListener('click',()=>closeSearch());overlay?.addEventListener('click',()=>{closeCart(false);closeSearch(false)});search?.addEventListener('click',e=>{if(e.target===search)closeSearch()});menuButton?.addEventListener('click',()=>{if(!menu)return;const open=menu.classList.toggle('is-open');menuButton.setAttribute('aria-expanded',open?'true':'false')});qa('.rb-header-nav a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeCart(false);closeSearch(false);closeMenu()}});const onScroll=()=>header?.classList.toggle('is-compact',scrollY>24);onScroll();addEventListener('scroll',onScroll,{passive:true});const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,items=qa('.rb-reveal');if(!reduced&&'IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -30px'});items.forEach(item=>observer.observe(item))}else items.forEach(item=>item.classList.add('is-visible'))})();
